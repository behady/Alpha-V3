# Project-Scoped Rules

- **Git Push Constraint**: Do NOT execute any `git push` commands unless the user explicitly tells you to do so (e.g. "push changes", "git push", etc.). You may stage (`git add`) and commit (`git commit`) changes locally, but keep the push step separate until requested.
