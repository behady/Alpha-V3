"use client";

import WhatsAppSettings from "@/components/settings/WhatsAppSettings";

/** Settings → WhatsApp: the number, the automatic messages, their wording, owner alerts. */
export default function WhatsAppChannelHost() {
  return <WhatsAppSettings section="channel" />;
}
