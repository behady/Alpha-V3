const fs = require('fs'); let lines = fs.readFileSync('src/components/dashboard/WeeklyScheduleView.tsx', 'utf8').split('\n'); lines.splice(47, 6, '            const h = Math.floor(m / 60);', '            const mins = m % 60;', '            const ampm = h >= 12 ? (language === \'ar\' ? \'?\' : \'PM\') : (language === \'ar\' ? \'?\' : \'AM\');', '            const h12 = h % 12 || 12;', '            const h12Str = language === \'ar\' ? h12.toLocaleString(\'ar-EG\', {minimumIntegerDigits: 2}) : h12.toString().padStart(2, \'0\');', '            const minsStr = language === \'ar\' ? mins.toLocaleString(\'ar-EG\', {minimumIntegerDigits: 2}) : mins.toString().padStart(2, \'0\');', '            const label = language === \'ar\' ? \${h12Str}: \ : \${h12Str}: \;', '            slots.push({ minutes: m, label });'); fs.writeFileSync('src/components/dashboard/WeeklyScheduleView.tsx', lines.join('\n'));
